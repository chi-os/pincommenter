{
  "$schema": "https://developers.reddit.com/schema/config-file.v1.json",
  "name": "pincommenter",
  "server": {
    "dir": "dist/server",
    "entry": "index.cjs"
  },
  "permissions": {
    "reddit": true,
    "redis": true
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "settings": {
    "subreddit": {
      "generalSetup": {
        "type": "group",
        "label": "1. General Setup & Exceptions",
        "fields": {
          "ignoreModerators": {
            "type": "boolean",
            "label": "Ignore Moderators",
            "defaultValue": true,
            "helpText": "If enabled, posts submitted by subreddit moderators will NOT receive any automated comments."
          },
          "ignoreApprovedUsers": {
            "type": "boolean",
            "label": "Ignore Approved Users",
            "defaultValue": false,
            "helpText": "If enabled, posts submitted by approved submitters will NOT receive any automated comments."
          },
          "pinComment": {
            "type": "boolean",
            "label": "Pin Bot Comment (Sticky)",
            "defaultValue": true,
            "helpText": "If enabled, the bot will automatically distinguish and sticky its generated comment to the top of the post."
          },
          "lockComment": {
            "type": "boolean",
            "label": "Lock Bot Comment",
            "defaultValue": false,
            "helpText": "If enabled, the bot comment will be locked so users cannot reply directly to it."
          },
          "excludedFlairs": {
            "type": "string",
            "label": "Ignored Post Flairs (Comma separated)",
            "defaultValue": "No Bot, Meta",
            "helpText": "Posts with these exact flairs will be ignored (case-insensitive). If an excluded flair is added later, pending timers are cancelled and bot comments are deleted."
          },
          "waitTimeSeconds": {
            "type": "number",
            "label": "Wait Time (Seconds)",
            "defaultValue": 60,
            "helpText": "Delay in seconds before the bot executes and comments on the post."
          }
        }
      },
      "standardCommentsSetup": {
        "type": "group",
        "label": "2. Standard Comment Setup",
        "fields": {
          "enableStandard": {
            "type": "boolean",
            "label": "Enable Standard Comment",
            "defaultValue": true,
            "helpText": "If enabled, posts will receive a standard comment unless excluded."
          },
          "standardComment": {
            "type": "string",
            "label": "Standard Comment Text",
            "defaultValue": "Hello {{author}}, thanks for your post!\n\nPlease remember to follow our community guidelines.",
            "helpText": "The default comment text to post. Use {{author}} for the username and \\n for line breaks/paragraphs."
          }
        }
      },
      "keywordCommentsSetup": {
        "type": "group",
        "label": "3. Keyword Triggered Setup",
        "fields": {
          "enableKeyword": {
            "type": "boolean",
            "label": "Enable Keyword Comments",
            "defaultValue": true,
            "helpText": "If enabled, posts matching specific keywords or regex patterns receive a dedicated comment."
          },
          "keywordRules": {
            "type": "string",
            "label": "Keyword Rules",
            "defaultValue": "help => Hello {{author}}, here is our help center!\n\nCheck our FAQ before asking questions.;; regex:\\b(bug|issue)\\b => Hi {{author}},\n\nPlease report bugs in our weekly tracker thread.",
            "helpText": "Format: 'keyword => comment'. For regex matching, use 'regex:pattern => comment'. Separate rules with double semicolons (;;). Use \\n for line breaks within a comment. First match wins."
          },
          "matchBehavior": {
            "type": "select",
            "label": "Match Behavior",
            "options": [
              { "label": "Only post keyword comment (Skip standard)", "value": "keyword_only" },
              { "label": "Post both (Standard + Keyword comment)", "value": "both" }
            ],
            "defaultValue": "keyword_only",
            "helpText": "Defines whether both standard and keyword comments are posted when a keyword matches."
          }
        }
      }
    }
  },
  "triggers": {
    "onPostSubmit": "/internal/triggers/on-post-submit",
    "onPostFlairUpdate": "/internal/triggers/on-post-flair-update"
  },
  "scheduler": {
    "tasks": {
      "postCommentJob": {
        "endpoint": "/internal/scheduler/post-comment-job"
      }
    }
  }
}